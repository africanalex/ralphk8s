# Gemini-Specific Instructions
- Use the Google Search grounding tool if you encounter unknown library versions.
- You have a massive context window; always keep the full content of `prd/{{ .Values.job.prd }}-prd-progress.txt` in your active reasoning.
- Use the terminal to execute the Ralph workflow.